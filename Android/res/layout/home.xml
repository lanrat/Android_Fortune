<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >
    
    <include android:id="@+id/current_fortune"
        android:layout_width="fill_parent"
    	android:layout_height="wrap_content"
        layout="@layout/fortune_layout" />
    <Button
        android:id="@+id/flag_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="testFlag"
        android:layout_below="@+id/current_fortune"
        android:text="Flag Current Fortune"/>
    <Button
        android:id="@+id/number_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="testNumberFortunes"
        android:layout_below="@+id/flag_button"
        android:text="Get # of local fortunes"/>

    <Button
        android:id="@+id/refresh_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="updateFortune"
        android:layout_alignParentBottom="true"
        android:text="@string/refresh_button"/>

</RelativeLayout>